services:
  odoo:
    build:
      context: .
      dockerfile: Dockerfile
    image: odoo:19.0-custom
    container_name: odoo_app_19
    restart: always
    ports:
      - "8069:8069"
      - "8071:8071"
    environment:
      - HOST=db
      - USER=user_mulai-digital-kreasi
      - PASSWORD=adminG@nt4ngMulaiAcaemy
      - ADMIN_PASSWORD=AdminG@nt4ng
      - LANG=en_US.UTF-8
      - LC_ALL=en_US.UTF-8
    volumes:
      - odoo-data:/var/lib/odoo
      - ./addons:/mnt/extra-addons
      - ./oca/storage:/mnt/oca-storage
      - ./oca/server-tools:/mnt/oca-server-tools
    networks:
      - app_network

volumes:
  odoo-data:

networks:
  app_network:
    external: true